import JSONP from 'jsonp'
JSONP(url, {}, function() {})
export default class HttpJsonP {
  static jsonp(options) {
    return new Promise(
      ((resolve, rejects) = {
        // JsonP(options, {params: 'callback'}, function (err, response) {
        // })
      })
    )
  }
}
